using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestFunctions : MonoBehaviour
{
    public Material material;
    // Start is called before the first frame update
    void Start()
    {
        /*
        RailStruct rs = new RailStruct(transform.position, transform.rotation, 0, 1.95f, 90, 10, true);
        GameObject RailOne = WorkBench.railPieceGenerator.GenerateAPiece(rs);
        RailOne.GetComponentInChildren<MeshRenderer>().material = material;
        WorkBench.railManager.AddRail(RailOne);

        //transform.position -= Vector3.forward;
        RailStruct rs2 = new RailStruct(transform.position, transform.rotation, 1.1f, 0, 0, 0, true);
        GameObject RailTwo = WorkBench.railPieceGenerator.GenerateAPiece(rs2);
        RailOne.GetComponentInChildren<MeshRenderer>().material = material;
        WorkBench.railManager.AddRail(RailTwo);
    */
        }

    // Update is called once per frame
    void Update()
    {
        
    }
}
